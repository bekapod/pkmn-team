import { Meta, Typeset } from '@storybook/addon-docs';
import { theme } from '../../tailwind.config.js';

<Meta title="Tokens/Typography" />

# Typography

## Thin

<Typeset
  fontSizes={Object.keys(theme.fontSize).map(size => theme.fontSize[size][0])}
  fontFamily={theme.fontFamily.sans}
  fontWeight={100}
  sampleText="Grumpy wizards make a toxic brew for the jovial queen."
/>

## Light

<Typeset
  fontSizes={Object.keys(theme.fontSize).map(size => theme.fontSize[size][0])}
  fontFamily={theme.fontFamily.sans}
  fontWeight={300}
  sampleText="Grumpy wizards make a toxic brew for the jovial queen."
/>

## Regular

<Typeset
  fontSizes={Object.keys(theme.fontSize).map(size => theme.fontSize[size][0])}
  fontFamily={theme.fontFamily.sans}
  sampleText="Grumpy wizards make a toxic brew for the jovial queen."
/>

## Medium

<Typeset
  fontSizes={Object.keys(theme.fontSize).map(size => theme.fontSize[size][0])}
  fontFamily={theme.fontFamily.sans}
  fontWeight={500}
  sampleText="Grumpy wizards make a toxic brew for the jovial queen."
/>

## Bold

<Typeset
  fontSizes={Object.keys(theme.fontSize).map(size => theme.fontSize[size][0])}
  fontFamily={theme.fontFamily.sans}
  fontWeight={700}
  sampleText="Grumpy wizards make a toxic brew for the jovial queen."
/>

## Black

<Typeset
  fontSizes={Object.keys(theme.fontSize).map(size => theme.fontSize[size][0])}
  fontFamily={theme.fontFamily.sans}
  fontWeight={900}
  sampleText="Grumpy wizards make a toxic brew for the jovial queen."
/>
